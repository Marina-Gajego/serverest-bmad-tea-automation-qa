{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Put users without ID response schema",
  "type": "object",
  "required": [
    "message"
  ],
  "additionalProperties": false,
  "properties": {
    "message": {
      "type": "string",
      "enum": [
        "Não é possível realizar PUT em /usuarios. Acesse http://localhost:3000 para ver as rotas disponíveis e como utilizá-las."
      ]
    }
  }
}
